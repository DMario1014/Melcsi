
const typed = new Typed('.multiple', {
    strings: ['2D Animator', 'Character Designer', 'Video Editor'],
    typeSpeed: 100,      // Gépelési sebesség
    backSpeed: 100,      // Törlési sebesség
    backDelay: 1000,     // Mennyi idő után törli
    loop: true           // Végtelen ciklus
});